# 🚀 دليل النشر على Vercel

## 📋 المتغيرات المطلوبة في Vercel

### المتغيرات الأساسية (مطلوبة فوراً):

| المتغير | القيمة | الوصف |
|---------|--------|--------|
| `NEXTAUTH_URL` | `https://leve1up.store` | رابط الموقع الأساسي |
| `NEXTAUTH_SECRET` | `your-32-character-secret-key` | مفتاح سري قوي للتشفير |

### متغيرات OAuth (اختيارية):

| المتغير | القيمة | متى تضيفه |
|---------|--------|-----------|
| `GOOGLE_CLIENT_ID` | من Google Cloud Console | عند تفعيل Google OAuth |
| `GOOGLE_CLIENT_SECRET` | من Google Cloud Console | عند تفعيل Google OAuth |
| `NEXT_PUBLIC_GOOGLE_ENABLED` | `true` | لإظهار زر Google |
| `APPLE_ID` | من Apple Developer | عند تفعيل Apple OAuth |
| `APPLE_SECRET` | من Apple Developer | عند تفعيل Apple OAuth |
| `NEXT_PUBLIC_APPLE_ENABLED` | `true` | لإظهار زر Apple |

### متغيرات قاعدة البيانات (اختيارية):

| المتغير | القيمة | متى تضيفه |
|---------|--------|-----------|
| `KV_REST_API_URL` | من Upstash Redis | عند استخدام Redis |
| `KV_REST_API_TOKEN` | من Upstash Redis | عند استخدام Redis |

---

## 🔧 خطوات الإعداد في Vercel

### 1. الوصول إلى إعدادات المشروع

1. اذهب إلى [Vercel Dashboard](https://vercel.com/dashboard)
2. اختر مشروع `leve1up`
3. اضغط على **Settings**
4. اختر **Environment Variables** من القائمة الجانبية

### 2. إضافة المتغيرات

لكل متغير:
1. اضغط على **Add New**
2. أدخل **Name** (اسم المتغير)
3. أدخل **Value** (قيمة المتغير)
4. اختر **Environments**:
   - ✅ Production
   - ✅ Preview  
   - ✅ Development
5. اضغط **Save**

### 3. إعادة النشر

بعد إضافة المتغيرات:
```bash
# عبر Vercel CLI
vercel --prod

# أو عبر Dashboard
# اذهب إلى Deployments > اضغط على أحدث deployment > Redeploy
```

---

## ⚡ إعداد سريع عبر CLI

```bash
# تسجيل الدخول
vercel login

# إضافة المتغيرات الأساسية
vercel env add NEXTAUTH_URL production
# أدخل: https://leve1up.store

vercel env add NEXTAUTH_SECRET production  
# أدخل: your-32-character-secret-key

# إعادة النشر
vercel --prod
```

---

## 🔍 التحقق من المتغيرات

### في Vercel Dashboard:
1. **Settings** > **Environment Variables**
2. تأكد من وجود جميع المتغيرات المطلوبة
3. تحقق من أن كل متغير مضاف لجميع البيئات

### في الموقع المنشور:
1. اذهب إلى `https://leve1up.store/register`
2. تحقق من ظهور الصفحة بدون أخطاء
3. إذا كان OAuth مفعلاً، ستظهر الأزرار

---

## 🛠️ استكشاف الأخطاء

### المشاكل الشائعة:

1. **صفحة 500 Internal Server Error**:
   - تحقق من وجود `NEXTAUTH_SECRET`
   - تأكد من صحة `NEXTAUTH_URL`

2. **OAuth لا يعمل**:
   - تحقق من إضافة `GOOGLE_CLIENT_ID` و `GOOGLE_CLIENT_SECRET`
   - تأكد من وجود `NEXT_PUBLIC_GOOGLE_ENABLED=true`
   - أعد النشر بعد إضافة المتغيرات

3. **المتغيرات لا تظهر**:
   - تأكد من إضافة المتغيرات لبيئة Production
   - أعد النشر بعد إضافة المتغيرات
   - انتظر بضع دقائق لتحديث الخادم

### فحص المتغيرات:

```javascript
// في أي صفحة Next.js
console.log('NEXTAUTH_URL:', process.env.NEXTAUTH_URL);
console.log('Google Enabled:', process.env.NEXT_PUBLIC_GOOGLE_ENABLED);
```

---

## 📱 الحالة الحالية

### ✅ يعمل حالياً:
- التسجيل التقليدي (email + password)
- تسجيل الدخول التقليدي
- حماية الصفحات
- تسجيل الخروج
- صفحة الملف الشخصي

### ⏳ جاهز للتفعيل:
- Google OAuth (يحتاج إعداد المتغيرات)
- Apple OAuth (يحتاج إعداد المتغيرات)
- Redis/KV للتخزين (اختياري)

---

## 🎯 الخطوات التالية

1. **أضف المتغيرات الأساسية** في Vercel Dashboard
2. **أعد النشر** للتأكد من عمل النظام
3. **اختبر التسجيل التقليدي** على الموقع المنشور
4. **أضف OAuth** عند الحاجة (راجع `OAUTH_SETUP.md`)

**الموقع جاهز للاستخدام مع التسجيل التقليدي فور إضافة المتغيرات الأساسية!** 🎉

