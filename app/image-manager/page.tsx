import ImageManager from '@/components/ImageManager';

export const metadata = {
  title: 'مدير صور المنتجات - Leve1Up Store',
  description: 'فحص وإدارة صور المنتجات في متجر Leve1Up',
};

export default function ImageManagerPage() {
  return <ImageManager />;
}
